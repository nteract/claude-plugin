# nteract Claude Code plugin (stable)

This repository is auto-generated by the [nteract/desktop](https://github.com/nteract/desktop) release pipeline. **Do not open pull requests here.** Edits will be overwritten on the next release.

## Installing

```
/plugin marketplace add nteract/claude-plugin
/plugin install nteract@claude-plugin
```

## Pinning a version

```
/plugin install nteract@claude-plugin --ref v2.3.0
```

Tags are published per release. `main` always points at the latest stable release.

## What lives here

- `plugins/nteract/bin/runt-proxy-<target>` — per-platform MCP proxy binaries (macOS arm64/x64, Linux x64, Windows x64)
- `plugins/nteract/bin/runt-proxy` — shell wrapper that dispatches to the right binary at runtime
- `plugins/nteract/{.mcp.json,.claude-plugin,skills}` — plugin manifests and skills, copied verbatim from `nteract/desktop`

Source: <https://github.com/nteract/desktop/tree/main/plugins/nteract>